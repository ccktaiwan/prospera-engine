# DISABLED 2026-05-07 - unified under prospera_guard.yml
name: Architecture Tests (Disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled"